import { InjectionMarkerDirective } from './injection-marker.directive';

describe('InjectionMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new InjectionMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
